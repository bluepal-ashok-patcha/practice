# Database Schema

This directory contains the `schema.sql` file, which defines the database schema for the real-time chat application. All microservices should connect to the same database and use this schema.

## How to Use

1. Create a MySQL database named `whatsapp_clone`.
2. Run the `schema.sql` script to create the necessary tables.
